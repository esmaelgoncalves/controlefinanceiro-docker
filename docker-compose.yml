version: '3.1'

services:
  mysql:
    image: mysql:5.7.25
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: controlefinanceiro

  controle-financeiro-api:
    build: .
    depends_on:
      - mysql
    links:
      - mysql
    ports:
      - 8080:8080